# Copie para .env (git-ignorado). Usado só pelos scripts locais em scripts/radar.
# A service role key ignora RLS: nunca colocar no frontend nem commitar.
SUPABASE_URL=https://hfjvwibucplyhsvnwfor.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Frontend (vai em .env.local, não aqui): GEMINI_API_KEY, VITE_PROXY_PNCP_URL,
# VITE_PROXY_EQ_URL, VITE_EMPRESAQUI_TOKEN. Ver CLAUDE.md.
